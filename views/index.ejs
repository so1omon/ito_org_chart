<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="referrer" content="no-referrer" />
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">  
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="css/main.css">
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/gh/moonspam/NanumSquare@1.0/nanumsquare.css">
    <script src="https://code.jquery.com/jquery-1.12.4.js"></script>
    <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
    <script type="text/javascript" src="./create.js"></script>
</head>
<body>
    <div id="container">
        <%-include('background.ejs') %>
        <div class="logo">
           <img src="img\logoFile.png" style="padding: 10px;">
        </div>
        <div class="main">
                <!-- 왼쪽 department-->
                <div class="office" id="first-office">
                    <div class="header fw-bold">
                        <span class="office-name fs-4">관광마케팅실</span>
                        <span class="office-head">김민혜 실장</span>
                    </div>
                    <div class="dept-container">
                        <!-- 각 팀 table -->
                        <div class="department fs-5 fw-bold " id="first-dept">
                            <span class="dept-name">해외마케팅팀</span>
                            <div class="dept-table p-2" id="first-table">
                                 <!-- 팀장 row -->
                                 <div class="row justify-content-md-center p-2" id="team-row" >
                                    
                                        <% for(var i=0;i<list.length;i++){ %>
                                        <% if(list[i].dept_name== "해외마케팅팀" && list[i].duty_name=="팀장"){ %>
                                            <div class="border bg-light cell mr-0" id="team-leader">
                                                <img src="<%= list[i].img_url %>" class="mem-img">
                                                </div>
                                              <div class="memInfo">
                                                <span class="memName fs-6" id="<%= list[i].emp_id %>"><%= list[i].emp_name %></span>
                                                <span class="memPos fs-6" ><%= list[i].duty_name %> </span>
                                          <%} %>
                                          <%}%>
                                      </div>
                                      
                                 </div>
                                 <div class="row justify-content-md-center g-2  mr-0" id="mem-row" style="float: none; margin:100 auto;">
                                    <!-- 사람 수대로 col 삽입. 한행당 2열인 경우 --> 
                                    <% for(var i=0;i<4;i++){%>
                                        <% for(var j=0;j<2;j++){%>
                                            
                                                <div class="col-6 text-center mem" id="<%= i*2+j+1 %>" style="float: none; margin:100 auto;">
                                                    <div class="border bg-light cell mr-0">
                                                        <% for(var m=0;m<list.length;m++){%>
                                                        <% var num = i*2+j+1  %>   
                                                        <%   if(list[m].dept_name=="해외마케팅팀" && list[m].seat_arrng==num){%>
                                                            <img src="<%= list[m].img_url %>" class="mem-img">
                                                            <%}%>
                                                            <%}%>
                                                    </div>
                                                    <div class="memInfo">
                                                        <% for(var k=0;k<list.length;k++){%>
                                                        <% var num = i*2+j+1  %>   
                                                        <%   if(list[k].dept_name=="해외마케팅팀" && list[k].seat_arrng==num){%>
                                                            <span class="memName fs-6" id = "<%= list[k].emp_id %>"><%= list[k].emp_name %></span>
                                                            <span class="memPos fs-6"><%= list[k].post_name %></span>
                                                            
                                                        <%}%>
                                                        <%}%>
                                                        
                                                    </div>
                                                </div>
                                            <%}%>
                                       <% }%>

                                </div>
                            </div>
                        </div>
                        <div class="department fs-5 fw-bold" id="second-dept" >
                            <span class="dept-name">국내관광팀</span>
                            <div class="dept-table p-2" id="second-table">
                                     <!-- 팀장 row -->
                                 <div class="row justify-content-md-center p-2" id="team-row" >
                                    
                                    <% for(var i=0;i<list.length;i++){ %>
                                    <% if(list[i].dept_name== "국내관광팀" && list[i].duty_name=="팀장"){ %>
                                        <div class="border bg-light cell mr-0" id="team-leader">
                                            <img src="<%= list[i].img_url %>" class="mem-img">
                                            </div>
                                          <div class="memInfo">
                                            <span class="memName fs-6" id="<%= list[i].emp_id %>"><%= list[i].emp_name %></span>
                                            <span class="memPos fs-6" ><%= list[i].duty_name %> </span>
                                      <%} %>
                                      <%}%>
                                  </div>
                                  
                             </div>
                                 <div class="row justify-content-md-center g-2 pd-2" id="mem-row"  style="float: none; margin:100 auto;">
                                    <!-- 사람 수대로 col 삽입. 한행당 2열인 경우 --> 
                                    <% for(var i=0;i<4;i++){%>
                                        <% for(var j=0;j<2;j++){%>
                                            
                                                <div class="col-6 text-center mem" id="<%= i*2+j+1 %>" style="float: none; margin:100 auto;">
                                                    <div class="border bg-light cell mr-0">
                                                        <% for(var m=0;m<list.length;m++){%>
                                                        <% var num = i*2+j+1  %>   
                                                        <%   if(list[m].dept_name=="국내관광팀" && list[m].seat_arrng==num){%>
                                                            <img src="<%= list[m].img_url %>" class="mem-img">
                                                            <%}%>
                                                            <%}%>
                                                    </div>
                                                    <div class="memInfo">
                                                        <% for(var k=0;k<list.length;k++){%>
                                                        <% var num = i*2+j+1  %>   
                                                        <%   if(list[k].dept_name=="국내관광팀" && list[k].seat_arrng==num){%>
                                                            <span class="memName fs-6" id = "<%= list[k].emp_id %>"><%= list[k].emp_name %></span>
                                                            <span class="memPos fs-6"><%= list[k].post_name %></span>
                                                            
                                                        <%}%>
                                                        <%}%>
                                                        
                                                    </div>
                                                </div>
                                            <%}%>
                                       <% }%>
                                </div>

                            </div>
                        </div>
                        <div class="department fs-5 fw-bold" id="third-dept">
                            <span class="dept-name">스마트관광팀</span>
                            <div class="dept-table p-2" id="third-table">
                                   <!-- 팀장 row -->
                                 <div class="row justify-content-md-center p-2" id="team-row" >
                                    
                                    <% for(var i=0;i<list.length;i++){ %>
                                    <% if(list[i].dept_name== "스마트관광팀" && list[i].duty_name=="팀장"){ %>
                                        <div class="border bg-light cell mr-0" id="team-leader">
                                            <img src="<%= list[i].img_url %>" class="mem-img">
                                            </div>
                                          <div class="memInfo">
                                            <span class="memName fs-6" id="<%= list[i].emp_id %>"><%= list[i].emp_name %></span>
                                            <span class="memPos fs-6" ><%= list[i].duty_name %> </span>
                                      <%} %>
                                      <%}%>
                                  </div>
                                  
                             </div>
                                 <div class="row justify-content-md-center g-2 pd-2" id="mem-row">
                                    <!-- 사람 수대로 col 삽입. 한행당 2열인 경우 --> 
                                    <% for(var i=0;i<4;i++){%>
                                        <% for(var j=0;j<3;j++){%>
                                            
                                                <div class="col-4 text-center mem" id="<%= i*3+j+1 %>" style="float: none; margin:100 auto;">
                                                    <div class="border bg-light cell mr-0">
                                                        <% for(var m=0;m<list.length;m++){%>
                                                        <% var num = i*3+j+1  %>   
                                                        <%   if(list[m].dept_name=="스마트관광팀" && list[m].seat_arrng==num){%>
                                                            <img src="<%= list[m].img_url %>" class="mem-img">
                                                            <%}%>
                                                            <%}%>
                                                    </div>
                                                    <div class="memInfo">
                                                        <% for(var k=0;k<list.length;k++){%>
                                                        <% var num = i*3+j+1  %>   
                                                        <%   if(list[k].dept_name=="스마트관광팀" && list[k].seat_arrng==num){%>
                                                            <span class="memName fs-6" id = "<%= list[k].emp_id %>"><%= list[k].emp_name %></span>
                                                            <span class="memPos fs-6"><%= list[k].post_name %></span>
                                                            
                                                        <%}%>
                                                        <%}%>
                                                        
                                                    </div>
                                                </div>
                                            <%}%>
                                       <% }%>
                                </div>
                            </div>
                        </div>
                        <div class="department fs-5 fw-bold " id="last-dept">
                            <span class="dept-name">MICE뷰로</span>
                            <div class="dept-table p-2" id="last-table">
                                     <!-- 팀장 row -->
                                 <div class="row justify-content-md-center p-2" id="team-row" >
                                    
                                        <% for(var i=0;i<list.length;i++){ %>
                                        <% if(list[i].dept_name== "MICE뷰로" && list[i].duty_name=="팀장"){ %>
                                            <div class="border bg-light cell mr-0" id="team-leader">
                                                <img src="<%= list[i].img_url %>" class="mem-img">
                                                </div>
                                              <div class="memInfo">
                                                <span class="memName fs-6" id="<%= list[i].emp_id %>"><%= list[i].emp_name %></span>
                                                <span class="memPos fs-6" ><%= list[i].duty_name %> </span>
                                          <%} %>
                                          <%}%>
                                      </div>
                                      
                                 </div>
                                 <div class="row no-gutters justify-content-md-center g-2 pd-2" id="mem-row" >
                                    <!-- 사람 수대로 col 삽입. 한행당 2열인 경우 --> 
                                    <% for(var i=0;i<4;i++){%>
                                        <% for(var j=0;j<2;j++){%>
                                            
                                                <div class="col-6 text-center mem" id="<%= i*2+j+1 %>" style="float: none; margin:100 auto;">
                                                    <div class="border bg-light cell mr-0">
                                                        <% for(var m=0;m<list.length;m++){%>
                                                        <% var num = i*2+j+1  %>   
                                                        <%   if(list[m].dept_name=="MICE뷰로" && list[m].seat_arrng==num){%>
                                                            <img src="<%= list[m].img_url %>" class="mem-img">
                                                            <%}%>
                                                            <%}%>
                                                    </div>
                                                    <div class="memInfo">
                                                        <% for(var k=0;k<list.length;k++){%>
                                                        <% var num = i*2+j+1  %>   
                                                        <%   if(list[k].dept_name=="MICE뷰로" && list[k].seat_arrng==num){%>
                                                            <span class="memName fs-6" id = "<%= list[k].emp_id %>"><%= list[k].emp_name %></span>
                                                            <span class="memPos fs-6"><%= list[k].post_name %></span>
                                                            
                                                        <%}%>
                                                        <%}%>
                                                        
                                                    </div>
                                                </div>
                                            <%}%>
                                       <% }%>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- 오른쪽 department -->
                <div class="office" id="second-office">
                    <div class="header fw-bold">
                        <span class="office-name fs-4">관광마케팅실</span>
                        <span class="office-head">김민혜 실장</span>
                    </div>
                    <div class="dept-container">
                        <!-- 각 팀 table -->
                        <div class="department fs-5 fw-bold " id="first-dept">
                            <span class="dept-name">고객홍보팀</span>
                            <div class="dept-table p-2" id="first-table">
                                 <!-- 팀장 row -->
                                 <div class="row justify-content-md-center p-2" id="team-row" >
                                    
                                        <% for(var i=0;i<list.length;i++){ %>
                                        <% if(list[i].dept_name== "고객홍보팀" && list[i].duty_name=="팀장"){ %>
                                            <div class="border bg-light cell mr-0" id="team-leader">
                                                <img src="<%= list[i].img_url %>" class="mem-img">
                                                </div>
                                              <div class="memInfo">
                                                <span class="memName fs-6" id="<%= list[i].emp_id %>"><%= list[i].emp_name %></span>
                                                <span class="memPos fs-6" ><%= list[i].duty_name %> </span>
                                          <%} %>
                                          <%}%>
                                      </div>
                                      
                                 </div>
                                 <div class="row justify-content-md-center g-2  mr-0" id="mem-row" style="float: none; margin:100 auto;">
                                    <!-- 사람 수대로 col 삽입. 한행당 2열인 경우 --> 
                                    <% for(var i=0;i<4;i++){%>
                                        <% for(var j=0;j<2;j++){%>
                                            
                                                <div class="col-6 text-center mem" id="<%= i*2+j+1 %>" style="float: none; margin:100 auto;">
                                                    <div class="border bg-light cell mr-0">
                                                        <% for(var m=0;m<list.length;m++){%>
                                                        <% var num = i*2+j+1  %>   
                                                        <%   if(list[m].dept_name=="고객홍보팀" && list[m].seat_arrng==num){%>
                                                            <img src="<%= list[m].img_url %>" class="mem-img">
                                                            <%}%>
                                                            <%}%>
                                                    </div>
                                                    <div class="memInfo">
                                                        <% for(var k=0;k<list.length;k++){%>
                                                        <% var num = i*2+j+1  %>   
                                                        <%   if(list[k].dept_name=="고객홍보팀" && list[k].seat_arrng==num){%>
                                                            <span class="memName fs-6" id = "<%= list[k].emp_id %>"><%= list[k].emp_name %></span>
                                                            <span class="memPos fs-6"><%= list[k].post_name %></span>
                                                            
                                                        <%}%>
                                                        <%}%>
                                                        
                                                    </div>
                                                </div>
                                            <%}%>
                                       <% }%>

                                </div>
                            </div>
                        </div>
                        <div class="department fs-5 fw-bold" id="second-dept" >
                            <span class="dept-name">전략기획팀</span>
                            <div class="dept-table p-2" id="second-table">
                                     <!-- 팀장 row -->
                                 <div class="row justify-content-md-center p-2" id="team-row" >
                                    
                                    <% for(var i=0;i<list.length;i++){ %>
                                    <% if(list[i].dept_name== "전략기획팀" && list[i].duty_name=="팀장"){ %>
                                        <div class="border bg-light cell mr-0" id="team-leader">
                                            <img src="<%= list[i].img_url %>" class="mem-img">
                                            </div>
                                          <div class="memInfo">
                                            <span class="memName fs-6" id="<%= list[i].emp_id %>"><%= list[i].emp_name %></span>
                                            <span class="memPos fs-6" ><%= list[i].duty_name %> </span>
                                      <%} %>
                                      <%}%>
                                  </div>
                                  
                             </div>
                                 <div class="row justify-content-md-center g-2 pd-2" id="mem-row"  style="float: none; margin:100 auto;">
                                    <!-- 사람 수대로 col 삽입. 한행당 2열인 경우 --> 
                                    <% for(var i=0;i<4;i++){%>
                                        <% for(var j=0;j<2;j++){%>
                                            
                                                <div class="col-6 text-center mem" id="<%= i*2+j+1 %>" style="float: none; margin:100 auto;">
                                                    <div class="border bg-light cell mr-0">
                                                        <% for(var m=0;m<list.length;m++){%>
                                                        <% var num = i*2+j+1  %>   
                                                        <%   if(list[m].dept_name=="전략기획팀" && list[m].seat_arrng==num){%>
                                                            <img src="<%= list[m].img_url %>" class="mem-img">
                                                            <%}%>
                                                            <%}%>
                                                    </div>
                                                    <div class="memInfo">
                                                        <% for(var k=0;k<list.length;k++){%>
                                                        <% var num = i*2+j+1  %>   
                                                        <%   if(list[k].dept_name=="전략기획팀" && list[k].seat_arrng==num){%>
                                                            <span class="memName fs-6" id = "<%= list[k].emp_id %>"><%= list[k].emp_name %></span>
                                                            <span class="memPos fs-6"><%= list[k].post_name %></span>
                                                            
                                                        <%}%>
                                                        <%}%>
                                                        
                                                    </div>
                                                </div>
                                            <%}%>
                                       <% }%>
                                </div>

                            </div>
                        </div>
                        <div class="department fs-5 fw-bold" id="last-dept">
                            <span class="dept-name">경영지원팀</span>
                            <div class="dept-table p-2" id="last-table">
                                   <!-- 팀장 row -->
                                 <div class="row justify-content-md-center p-2" id="team-row" >
                                    
                                    <% for(var i=0;i<list.length;i++){ %>
                                    <% if(list[i].dept_name== "경영지원팀" && list[i].duty_name=="팀장"){ %>
                                        <div class="border bg-light cell mr-0" id="team-leader">
                                            <img src="<%= list[i].img_url %>" class="mem-img">
                                            </div>
                                          <div class="memInfo">
                                            <span class="memName fs-6" id="<%= list[i].emp_id %>"><%= list[i].emp_name %></span>
                                            <span class="memPos fs-6" ><%= list[i].duty_name %> </span>
                                      <%} %>
                                      <%}%>
                                  </div>
                                  
                             </div>
                                 <div class="row justify-content-md-center g-2 pd-2" id="mem-row">
                                    <!-- 사람 수대로 col 삽입. 한행당 2열인 경우 --> 
                                    <% for(var i=0;i<4;i++){%>
                                        <% for(var j=0;j<4;j++){%>
                                            
                                                <div class="col-3 text-center mem" id="<%= i*4+j+1 %>" style="float: none; margin:100 auto;">
                                                    <div class="border bg-light cell mr-0">
                                                        <% for(var m=0;m<list.length;m++){%>
                                                        <% var num = i*4+j+1  %>   
                                                        <%   if(list[m].dept_name=="경영지원팀" && list[m].seat_arrng==num){%>
                                                            <img src="<%= list[m].img_url %>" class="mem-img">
                                                            <%}%>
                                                            <%}%>
                                                    </div>
                                                    <div class="memInfo">
                                                        <% for(var k=0;k<list.length;k++){%>
                                                        <% var num = i*4+j+1  %>   
                                                        <%   if(list[k].dept_name=="경영지원팀" && list[k].seat_arrng==num){%>
                                                            <span class="memName fs-6" id = "<%= list[k].emp_id %>"><%= list[k].emp_name %></span>
                                                            <span class="memPos fs-6"><%= list[k].post_name %></span>
                                                            
                                                        <%}%>
                                                        <%}%>
                                                        
                                                    </div>
                                                </div>
                                            <%}%>
                                       <% }%>
                                </div>
                            </div>
                        </div>
                        
                    </div>
                </div>
        </div>
    </div>
    
    <script>
        

    </script>
</body>
</html>